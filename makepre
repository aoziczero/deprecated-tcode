CXX=/usr/bin/g++-5
AR=/usr/bin/ar
RANLIB=/usr/bin/ranlib
RM=rm
MKDIR=mkdir -p
DEPEND_FILE=depend

ifeq ( $(RELEASE),1)
OBJS_DIR  = ./../build/release
DBG_FLAGS = -DNDEBUG 
else
OBJS_DIR  = ./../build/debug
DBG_FLAGS = -DDEBUG -g
endif

LIB_INCLUDES= -I./../

